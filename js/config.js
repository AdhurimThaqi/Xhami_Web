// Supabase project configuration.
// These two values are PUBLIC by design (they ship to every visitor's
// browser). Access control lives in the database's Row Level Security
// policies, never here. The secret (sb_secret_...) key must NEVER
// appear in this repository.
//
// Leave both empty to run the site in offline demo mode (localStorage).
window.HDF_CONFIG = {
  SUPABASE_URL: 'https://wsqdezovibzicvkubbuq.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_QPgbDr4oJvRbn2z-HIeuJg_tAmRHZPE',
};
